#include <stdio.h>
int main(void){

    unsigned int a = 5, mask = 1, tc;
    mask <<= 1;
    tc = a & mask;
    printf("%u mask is %u", tc, mask);
return 0;
}



for(int i; i<n; i++){
            if((a[i]&mask) == tc){

            }
        }
